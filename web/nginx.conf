server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript
               text/xml application/xml application/xml+rss text/javascript
               image/svg+xml;
    gzip_min_length 1024;

    # Proxy API requests to the backend
    # docker-compose passes VITE_API_URL=/api so all API calls go to /api/v1/...
    # nginx strips /api and forwards to the falcn-api service.
    location /api/ {
        rewrite ^/api(/.*)$ $1 break;
        proxy_pass http://falcn-api:8080;

        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # SSE / long-lived connections
        proxy_set_header Connection  '';
        proxy_http_version           1.1;
        proxy_buffering              off;
        proxy_cache                  off;
        proxy_read_timeout           3600s;
    }

    # Health check passthrough
    location /health {
        proxy_pass http://falcn-api:8080/health;
        proxy_set_header Host $host;
    }

    # Static assets — long cache TTL
    location ~* \.(js|css|woff2?|ttf|eot|svg|ico|png|jpg|jpeg|gif|webp)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # SPA routing — fallback to index.html for all unmatched paths
    location / {
        try_files $uri $uri/ /index.html;
    }
}
