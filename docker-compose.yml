version: '3.8'

services:
  # Web Frontend
  Falcn-web:
    build:
      context: ./web
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=/api
    container_name: Falcn-web
    ports:
      - "3000:80"
    restart: unless-stopped
    depends_on:
      - postgres
      - Falcn-api
    networks:
      - Falcn-network

  # API Backend
  Falcn-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: Falcn-api
    ports:
      - "8082:8080"
    restart: unless-stopped
    environment:
      - PORT=8080
      - ENV=production
      - FALCN_APP_ENVIRONMENT=production
      # SECURITY: set API_KEYS or FALCN_JWT_PRIVATE_KEY_FILE before deploying.
      # Copy .env.example → .env and fill in all REQUIRED values.
      - API_AUTH_ENABLED=${API_AUTH_ENABLED:-true}
      - API_KEYS=${API_KEYS:-}
      - FALCN_JWT_PRIVATE_KEY_FILE=${FALCN_JWT_PRIVATE_KEY_FILE:-}
      - Falcn_DB_TYPE=${Falcn_DB_TYPE:-sqlite}
      - Falcn_DB_HOST=${Falcn_DB_HOST:-postgres}
      - Falcn_DB_PORT=${Falcn_DB_PORT:-5432}
      - Falcn_DB_NAME=${Falcn_DB_NAME:-/app/data/Falcn.db}
      - Falcn_DB_USER=${Falcn_DB_USER:-Falcn}
      - Falcn_DB_PASSWORD=${Falcn_DB_PASSWORD:-}
      - Falcn_DB_SSLMODE=${Falcn_DB_SSLMODE:-disable}
      - Falcn_LOG_LEVEL=${Falcn_LOG_LEVEL:-info}
      - Falcn_CORS_ORIGINS=${Falcn_CORS_ORIGINS:-http://localhost:3000}
      - Falcn_DISABLE_AUTH=${Falcn_DISABLE_AUTH:-false}
    volumes:
      - Falcn_data:/app/data
      - Falcn_logs:/app/logs
    networks:
      - Falcn-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    depends_on:
      postgres:
        condition: service_healthy

  Falcn-seed:
    image: curlimages/curl:8.10.1
    container_name: Falcn-seed
    profiles:
      - default
    environment:
      - API_URL=http://Falcn-api:8080
    volumes:
      - ./deploy/seed.sh:/seed.sh:ro
    entrypoint: ["sh", "/seed.sh"]
    depends_on:
      Falcn-api:
        condition: service_healthy
    networks:
      - Falcn-network

  # API Backend (SQLite profile)
  Falcn-api-sqlite:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: Falcn-api-sqlite
    profiles:
      - sqlite
    ports:
      - "8080:8080"
    restart: unless-stopped
    environment:
      - PORT=8080
      - ENV=production
      - FALCN_APP_ENVIRONMENT=production
      - API_AUTH_ENABLED=${API_AUTH_ENABLED:-true}
      - API_KEYS=${API_KEYS:-}
      - FALCN_JWT_PRIVATE_KEY_FILE=${FALCN_JWT_PRIVATE_KEY_FILE:-}
      - Falcn_DB_TYPE=sqlite
      - Falcn_DB_NAME=/app/data/Falcn.db
      - Falcn_LOG_LEVEL=${Falcn_LOG_LEVEL:-info}
      - Falcn_CORS_ORIGINS=${Falcn_CORS_ORIGINS:-http://localhost:3000}
      - Falcn_DISABLE_AUTH=${Falcn_DISABLE_AUTH:-false}
    volumes:
      - Falcn_data:/app/data
      - Falcn_logs:/app/logs
    networks:
      - Falcn-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  Falcn-seed-sqlite:
    image: curlimages/curl:8.10.1
    container_name: Falcn-seed-sqlite
    profiles:
      - sqlite
    environment:
      - API_URL=http://Falcn-api-sqlite:8080
    volumes:
      - ./deploy/seed.sh:/seed.sh:ro
    entrypoint: ["sh", "/seed.sh"]
    depends_on:
      Falcn-api-sqlite:
        condition: service_healthy
    networks:
      - Falcn-network

  # API Backend (Postgres profile)
  Falcn-api-postgres:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: Falcn-api-postgres
    profiles:
      - postgres
    ports:
      - "8080:8080"
    restart: unless-stopped
    environment:
      - PORT=8080
      - ENV=production
      - FALCN_APP_ENVIRONMENT=production
      - API_AUTH_ENABLED=${API_AUTH_ENABLED:-true}
      - API_KEYS=${API_KEYS:-}
      - FALCN_JWT_PRIVATE_KEY_FILE=${FALCN_JWT_PRIVATE_KEY_FILE:-}
      - Falcn_DB_TYPE=postgres
      - Falcn_DB_HOST=postgres
      - Falcn_DB_PORT=5432
      - Falcn_DB_NAME=Falcn
      - Falcn_DB_USER=Falcn
      - Falcn_DB_PASSWORD=${Falcn_DB_PASSWORD:?FALCN_DB_PASSWORD must be set}
      - Falcn_DB_SSLMODE=${Falcn_DB_SSLMODE:-require}
      - Falcn_LOG_LEVEL=${Falcn_LOG_LEVEL:-info}
      - Falcn_CORS_ORIGINS=${Falcn_CORS_ORIGINS:-http://localhost:3000}
      - Falcn_DISABLE_AUTH=${Falcn_DISABLE_AUTH:-false}
    volumes:
      - Falcn_data:/app/data
      - Falcn_logs:/app/logs
    networks:
      - Falcn-network
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  Falcn-seed-postgres:
    image: curlimages/curl:8.10.1
    container_name: Falcn-seed-postgres
    profiles:
      - postgres
    environment:
      - API_URL=http://Falcn-api-postgres:8080
    volumes:
      - ./deploy/seed.sh:/seed.sh:ro
    entrypoint: ["sh", "/seed.sh"]
    depends_on:
      Falcn-api-postgres:
        condition: service_healthy
    networks:
      - Falcn-network

  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: Falcn-postgres
    environment:
      - POSTGRES_DB=Falcn
      - POSTGRES_USER=Falcn
      - POSTGRES_PASSWORD=${Falcn_DB_PASSWORD:?FALCN_DB_PASSWORD must be set}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: unless-stopped
    networks:
      - Falcn-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U Falcn -d Falcn"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Prometheus for metrics collection (optional)
  prometheus:
    image: prom/prometheus:latest
    container_name: Falcn-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./config/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--storage.tsdb.retention.time=200h'
      - '--web.enable-lifecycle'
    restart: unless-stopped
    networks:
      - Falcn-network
    profiles:
      - monitoring

  # Grafana for monitoring dashboards (optional)
  grafana:
    image: grafana/grafana:latest
    container_name: Falcn-grafana
    ports:
      - "3001:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD:?GRAFANA_PASSWORD must be set}
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana_data:/var/lib/grafana
      - ./monitoring/grafana/provisioning/dashboards:/var/lib/grafana/dashboards
      - ./monitoring/grafana/provisioning/datasources:/etc/grafana/provisioning/datasources
    restart: unless-stopped
    networks:
      - Falcn-network
    profiles:
      - monitoring

  # Node Exporter for system metrics (optional)
  node-exporter:
    image: prom/node-exporter:latest
    container_name: Falcn-node-exporter
    ports:
      - "9100:9100"
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    command:
      - '--path.procfs=/host/proc'
      - '--path.rootfs=/rootfs'
      - '--path.sysfs=/host/sys'
      - '--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)'
    restart: unless-stopped
    networks:
      - Falcn-network
    profiles:
      - monitoring

volumes:
  Falcn_data:
    driver: local
  Falcn_logs:
    driver: local
  postgres_data:
    driver: local
  prometheus_data:
    driver: local
  grafana_data:
    driver: local

networks:
  Falcn-network:
    driver: bridge


