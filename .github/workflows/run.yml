name: Run Falcn

on:
  workflow_dispatch: {}

jobs:
  run:
    runs-on: ubuntu-latest
    env:
      GOFLAGS: -mod=mod
      GOPRIVATE: github.com/falcn-io/*
      GONOSUMDB: github.com/falcn-io/*
      GOPROXY: direct
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.24'
      - name: Build
        run: |
          go mod download || true
          go build -o Falcn .
      - name: Run version
        run: |
          ./Falcn version || go run . version || true
      - name: Run sample scan
        run: |
          ./Falcn scan ./tests/e2e/test-projects/npm-vulnerable --output json > run_output.json || echo '{}' > run_output.json
      - name: Upload run output
        uses: actions/upload-artifact@v4
        with:
          name: run-output
          path: run_output.json



